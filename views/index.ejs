<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>URL Shortener</title>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="./style.css">
</head>
<body>
  <div class="container">
    <h1>URL Shortener</h1>
    <form action="/" method="POST">
      <label for="url">Enter the URL</label>
      <input type="url" id="url" name = "url" placeholder="e.g., https://www.example.com/long-page" required>
      <label for="keyword">Enter your keyword (optional)</label>
      <input type="text" id="keyword" name="shortCode" placeholder="e.g., my-short-link">
      <button type="submit">SUBMIT</button>
    </form>
    <ul id="list">
        <% links.map(({shortCode,url})=>{ %>
          <li><a href = "<%= url %>" target = "_blank"><%=host%>/<%= shortCode%></a></li>
      <% }) %>
    </ul>
  </div>
</body>
<script>
</script>
</html>
